const INITIAL_STATE = {
    data: []
};

export function tasksReducer(state = INITIAL_STATE, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default tasksReducer;